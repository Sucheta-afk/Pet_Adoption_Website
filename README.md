# 🐾 Pet Adoption Center - Streamlit + MySQL

A DBMS project that manages pets, parents, veterinarians, medical reports, and more — built using **Streamlit** and **MySQL**.

---

## 💻 Setup Instructions

## 1. Clone this repo:
   ```bash
   git clone https://github.com/<your-username>/PetAdoptionCenter.git
   cd PetAdoptionCenter
   ```
## 2. Install dependencies:
`
pip install -r requirements.txt
`

## 3. Import database:

Open MySQL Workbench → Data Import → Import pet_adoption.sql.

## 4. Run the app:
`
streamlit run app.py
`
